<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.3.9</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.satyam</groupId>
	<artifactId>testCaseManagement</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>testCaseManagement</name>
	<description>Test Case Management Service – Backend Development Project  This project involves building a Test Case Management Service as part of a backend development assignment. The service will allow users to create, read, update, and delete test cases within a test management system. The test cases will include essential information such as the test case title, description, priority, and execution status.  The service will be developed using Java 8+ and Spring Boot for building robust and scalable backend services. MongoDB will be used as the database to store test case information, leveraging its flexibility and scalability. The system will expose REST APIs to interact with the test cases, ensuring easy integration and communication with other systems or front-end interfaces.  Key objectives of this project include:  Following best design principles and appropriate design patterns to ensure the service is scalable, maintainable, and efficient. Ensuring clean, readable, and well-documented code that is easy to understand and extend. Implementing appropriate error handling and validation mechanisms for API robustness. Using Spring Boot to develop the RESTful services and MongoDB to store and retrieve data effectively. This project showcases the ability to design and implement backend systems that are reliable, easy to maintain, and capable of handling real-world test management tasks.</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>17</java.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-mongodb</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.modelmapper</groupId>
			<artifactId>modelmapper</artifactId>
			<version>3.0.0</version>  <!-- Check for latest version -->
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<version>1.18.28</version> <!-- Use latest version -->
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>2.3.0</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
